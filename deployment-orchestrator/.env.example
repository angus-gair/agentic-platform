# Environment Variables for ajinsights.app Deployment
# Copy this file to .env and fill in the actual values
# .env
# -----------------------------------------------------------------------------

# === LLM Provider Configuration ===
# Your OpenAI API key is required.
OPENAI_API_KEY=

# Optional: Keys for other LLM providers
ANTHROPIC_API_KEY=
XAI_API_KEY=
DEEPSEEK_API_KEY=

# Default LLM provider and model
LLM_PROVIDER=api.x.ai
LLM_MODEL=grok-3-mini

# === Database Configuration ===
# PostgreSQL settings
POSTGRES_USER=admin
POSTGRES_PASSWORD=
POSTGRES_DB=agentic_platform

# Redis settings
REDIS_PASSWORD=

# === Monitoring Configuration ===
# Prometheus credentials
PROMETHEUS_USER=prom
PROMETHEUS_PASSWORD=

# Grafana credentials
GF_ADMIN_USER=admin
GF_ADMIN_PASSWORD=

# Set to "true" to enable the /metrics endpoint in the backend
ENABLE_METRICS=true

# === Frontend Configuration ===
# These are set within the docker-compose.yml for production,
# but are listed here for reference.
# NEXT_PUBLIC_API_URL=https://agentic-ai.ajinsights.com.au/api
# NEXT_PUBLIC_WS_URL=wss://agentic-ai.ajinsights.com.au/ws

# === DockerHub ===
DOCKERHUB_USERNAME=angusjamesgair
DOCKERHUB_PASSWORD=
DOCKERHUB_PERSONAL_ACCESS_TOKEN=